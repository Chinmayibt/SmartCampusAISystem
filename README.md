# Smart Campus AI System

Public landing page for the project.

- **Frontend ticket:** `MM-0141B539` — external `css/styles.css` and `js/main.js` (stable vanilla stack).
- **Earlier ticket:** `MM-E13CF4A9`
- **Entry:** open `index.html` in your browser or deploy the folder as static hosting (keep `css/` and `js/` paths intact).
- **Meta:** description and theme color for sharing and mobile browsers.
